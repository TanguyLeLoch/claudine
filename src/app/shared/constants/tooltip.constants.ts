import { TooltipOptions } from 'primeng/api';

/**
 * Default tooltip options used across the application
 */
export const DEFAULT_TOOLTIP_OPTIONS: TooltipOptions = {
  tooltipPosition: 'bottom',
  tooltipStyleClass: '!min-w-10',
  showDelay: 200,
  hideDelay: 200,
};
